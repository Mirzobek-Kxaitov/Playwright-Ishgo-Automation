[pytest]
# Local development: headed mode with slowmo
# Screenshot va video har doim yozilib turadi
addopts = --screenshot on --video on --headed --slowmo 500

# ESLATMA: CI/CD da headless mode uchun quyidagini ishlating:
# pytest tests/ --screenshot on --video retain-on-failure
# (--headed va --slowmo ni olib tashlang)
