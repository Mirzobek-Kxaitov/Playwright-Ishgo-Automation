[pytest]
# Local development: headed mode with slowmo
# CI/CD: headless mode (use --headed flag to override)
addopts = --screenshot on --video retain-on-failure
# Note: Remove --headed for CI/CD headless mode
# Add --slowmo 1000 for local debugging if needed
